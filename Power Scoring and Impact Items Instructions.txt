1. Page & Data Model SetupTask for IDE: "Create a new report page titled 'High Impact Items'. Define the CustomKeywordMetrics interface to include powerScore, histAvg, histMin, histMax, impactImproved, lowVolume, and visibilityLoss."2. Power Score Calculation (Null & Zero Handling)The IDE must implement the following logic to ensure the Power Score correctly handles edge cases as requested:Logic Definition:Null Keyword: If the keyword string is null, undefined, or empty, the powerScore must be null.Zero/Negative Volume: If searchVolume (Column J) is $\le 0$, the powerScore must be 0.Calculation:If currentRank < 11: log10(searchVolume) * 10If currentRank is 11–20: log10(searchVolume) * (21 - currentRank)If currentRank > 20: The result should be allowed to go negative (to deprioritize deep-rank keywords).Task for IDE: "Implement the computePowerScore function. Ensure it explicitly returns null for missing keywords and 0 for zero search volume to prevent calculation errors."3. High Volume & Decline Flagging (Inspection Logic)To meet the goal of flagging keywords with the highest volume and biggest declines for inspection:Logic Definition:Decline Identification (Visibility Loss): * Lost Top 5: If previous90dRank $\le 6$ AND currentRank $\ge 5$.Lost Top 10: If previous90dRank $\le 10$ AND currentRank $\ge 11$.High Impact Flag: Label as "High Impact" only if searchVolume > 0 AND powerScore > 20.Task for IDE: "On the 'High Impact Items' page, implement a default sort that prioritizes keywords where visibilityLoss is NOT empty AND searchVolume is in the top 25th percentile. High-volume keywords that have dropped out of the Top 5 or Top 10 should be highlighted with an 'Inspection Required' badge."4. UI ImplementationTask for IDE: "Render the 'High Impact Items' table. Add a visual alert (e.g., a red border or exclamation icon) for keywords meeting the 'Inspection' criteria. Ensure the Power Score column is sortable and uses 'warning' styling for any negative values."5. ValidationTask for IDE: "Verify the logic against the [Validation Checklist](https://github.com/mitchterrau-sys/terra-SEO-dashboard/blob/main/User Assets/Custom Metrics Integration Instructions.md#L217-L228). Specifically, confirm that a keyword with 5,000 volume that dropped from rank 4 to rank 12 triggers both a 'Lost Top 10' flag and a high Power Score."